<--books.hbs-->
<p>Olá, {{ currentUser.name }} {{#if currentUser.isAdmin}} | <a href="/books/add">Add Book</a> {{/if}}| <a href="/logout">Logout</a></p>
<h1>BOOKS</h1>
{{#each books}}
    <p><a href="/book/{{_id}}">{{name}}</a></p>
    {{#if owned}}
        <p><a href="/books/edit?book_id={{_id}}" class="edit-button">Edit</a></p>
    {{/if}}
{{/each}}